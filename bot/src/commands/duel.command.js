// duel.command.js (заглушка - логика перенесена в handlers/duel/)

/**
 * Команды дуэлей - теперь обрабатываются в handlers/duel/
 * Этот файл оставлен для совместимости
 */

function duelCommand() {
  // Заглушка - команды дуэлей теперь обрабатываются в handlers/duel/group-duel.handler.js
  console.log('⚠️ duelCommand() вызвана, но логика перенесена в handlers/duel/');
}

function duelPersonalCommand() {
  // Заглушка - команды дуэлей теперь обрабатываются в handlers/duel/group-duel.handler.js
  console.log('⚠️ duelPersonalCommand() вызвана, но логика перенесена в handlers/duel/');
}

function duelHelpCommand() {
  // Заглушка - команды дуэлей теперь обрабатываются в handlers/duel/group-duel.handler.js
  console.log('⚠️ duelHelpCommand() вызвана, но логика перенесена в handlers/duel/');
}

function duelStatsCommand() {
  // Заглушка - команды дуэлей теперь обрабатываются в handlers/duel/group-duel.handler.js
  console.log('⚠️ duelStatsCommand() вызвана, но логика перенесена в handlers/duel/');
}

function duelHistoryCommand() {
  // Заглушка - команды дуэлей теперь обрабатываются в handlers/duel/group-duel.handler.js
  console.log('⚠️ duelHistoryCommand() вызвана, но логика перенесена в handlers/duel/');
}

function duelCancelCommand() {
  // Заглушка - команды дуэлей теперь обрабатываются в handlers/duel/group-duel.handler.js
  console.log('⚠️ duelCancelCommand() вызвана, но логика перенесена в handlers/duel/');
}

module.exports = {
  duelCommand,
  duelPersonalCommand,
  duelHelpCommand,
  duelStatsCommand,
  duelHistoryCommand,
  duelCancelCommand
};